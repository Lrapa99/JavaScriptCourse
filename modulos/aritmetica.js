function sumar(a, b ){
    return a + b;
}

function restar(a, b ){
    return a - b;
}

export const aritmetica = { //almacenamos dentro de un objeto todos los valores que deseamos exportar
    sumar,
    restar
}
